"""Compatibility entry point for the unified chapter/intermission regression.

Supports the same --mod, --case, --language and --renderer options as
 test_transitions.py. The campaign no longer opens the engine stat screen.
"""
from test_transitions import main

if __name__=='__main__':main()
