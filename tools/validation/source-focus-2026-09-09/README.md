# Source focus — runtime evidence

Direct UZDoom 5.0.1 captures from the isolated tested package. No mockups or
retouching. The core beam fades to 10% of its former intensity around the seal;
the helix fades to 35%. Both return smoothly above and below the target.
A separate 1100-unit additive glow layer surrounds the 740–800-unit rune ring,
with a broad amber falloff and a brighter compact centre. This adds one local
visual layer and no new actors with collision, dynamic lights or random calls.

OpenGL/Vulkan, open/closed shield, twelve layers after save/load, attack cues,
zero/reduced FX, death, TNT04C isolation and both cooperative peers are checked.
The room-expansion idea (a broken overhead rune seal) is not part of this patch.
