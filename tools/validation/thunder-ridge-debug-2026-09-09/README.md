# TNT02 ridge fog and public debug controls

Actual game screenshots show the silhouette-following dark crest band with fog retained below. Vulkan: 26 assertions using the public aliases, all profiles, both sound selections, explicit direction, exact thunder delay, save/load with pending distant thunder, cancelling pending events, invalid arguments and automatic-mode recovery. OpenGL: 2 assertions plus dark/skybox-seam screenshots. Audio disabled in automated checks; no listening test claimed.

The material and software fallback use the same 0.20 to 1.0 contour-relative fog attenuation. The source artwork, map geometry and map bytecode are unchanged. The height strip is derived technical data. Manual testing does not bind keys; commands and persistence behavior are documented in UTNT_THUNDER.md.
