# Ability borders and class cards, 2026-09-09

UZDoom 5.0.1: six successful runs (all three classes on Vulkan and OpenGL),
303 runtime assertions, and 66 successful pixel comparisons. Screenshots here
are captured from the engine. The full capture set is generated by the runner.

Coverage: pulse peaks/troughs/repeated phases, exact unchanged image center,
steady armor, reduced-effects mode, expiry, cutscenes, external cameras,
frozen player, save/load, map travel, respawn and automap. Cards: English/German,
640x480, 960x540, 1920x1080, 2560x1080, line wrapping, screen bounds, keyboard
selection and mouse selection through the newly enlarged ability section.

These tests use the committed base plus this contribution, excluding concurrent
unrelated edits. This presentation change does not alter ability gameplay.
No new campaign playthrough, WAN session or performance benchmark is claimed.

Run tools/test_ability_edges.py once per class/backend (add --cards for Marine,
--sizes for Scout/Commando), then tools/check_ability_edge_images.py on the logs.
